# Database (Turso) — leave as file:local.db for local development
TURSO_DATABASE_URL=file:local.db
TURSO_AUTH_TOKEN=

# Better Auth — required
# Generate a strong random secret (e.g. `openssl rand -base64 32`)
BETTER_AUTH_SECRET=change-this-to-a-random-string
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Google OAuth — get from https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: ${BETTER_AUTH_URL}/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Resend — required (used for verification + password reset + drift alerts)
# EMAIL_FROM must use a Resend-verified domain (SPF + DKIM + DMARC published).
# The sandbox `*@resend.dev` only delivers to your own inbox — do not use in prod.
RESEND_API_KEY=
EMAIL_FROM="FolioX Tracker <no-reply@mail.folioxtracker.com>"
EMAIL_REPLY_TO=hello@folioxtracker.com
# Optional: enables List-Unsubscribe header on rebalance alerts
EMAIL_UNSUBSCRIBE_MAILTO=unsubscribe@folioxtracker.com

# One-shot multi-user migration: address to use as the legacy owner.
# Falls back to settings.email if a row exists. Only used by `npm run migrate:multiuser`.
OWNER_EMAIL=

# Cron auth — server-to-server bearer for /api/cron/*
CRON_SECRET=change-this-to-a-random-string

# Admin metrics — comma-separated emails allowed to view /admin/metrics.
# Leave empty to deny access to everyone (the dashboard returns 404).
ADMIN_EMAILS=

# IMAP — for auto-importing CMC email confirmations (optional)
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=
IMAP_PASSWORD=

# Sentry — error tracking (optional but recommended in production)
# Create a project at https://sentry.io and paste the DSN below.
# Leave SENTRY_DSN blank to fully disable Sentry (no init, no source-map upload).
# NEXT_PUBLIC_SENTRY_DSN is the same value, exposed to the browser bundle.
# SENTRY_AUTH_TOKEN + ORG + PROJECT enable source-map upload at build time.
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
